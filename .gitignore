# 忽略git版本控制的檔案位置
gitignore/

# Python 快取
__pycache__/
*.pyc

# 機敏設定（環境變數）
.env

# 預覽頁：不用跑完整對局也能單獨觀察遊戲中的視覺元素
# 每個檔案獨立命名為「來源檔_觀察的部分.html」（例如 game_board.html）。
# 是個人的開發輔助工具，各自建各自的，不進版控。
PyPoly/static/preview/

# 107 個地點的經緯度/海拔匯入用 SQL。組員都是連 ngrok 連到同一台共用
# 伺服器測試，本機各自跑資料庫的情況很少，所以這份一次性匯入檔不進版控，
# 資料已經直接寫進共用 dev DB 了。
PyPoly/scenarios_geo_data.sql

# 實測遊玩時發現的已知問題清單（個人筆記，不進版控）。
prob.md
