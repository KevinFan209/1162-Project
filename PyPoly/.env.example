# PyPoly 環境變數範本
# 使用方式：複製本檔為 .env（同目錄），填入實際值。.env 已被 .gitignore 忽略，不會進版控。

# JWT 簽章密鑰（請改成隨機長字串）
SECRET_KEY=change-me-to-a-random-secret

# Gmail SMTP（忘記密碼寄送 OTP 用）
# SENDER_PASSWORD 需使用 Google 帳戶的「應用程式密碼」，非登入密碼。
# ⚠️ 舊版曾把密碼寫死在 main.py，請至 Google 帳戶撤銷該密碼並重新產生後填入此處。
SENDER_EMAIL=your-service@gmail.com
SENDER_PASSWORD=your-google-app-password

# 環境部空氣品質開放資料 API 金鑰
# 申請：https://data.moenv.gov.tw/  （會員中心 → API 金鑰）
# 冒險格會依該地監測站的即時 AQI 調整地價與過路費；未填寫時會使用保底值，
# 遊戲仍可正常進行，只是空氣品質不會反映真實數據。
# ⚠️ 切勿把金鑰寫進 .env.example 或任何進版控的檔案。
MOENV_API_KEY=

# 資料庫連線字串。
#
# ⚠️ 本機開發請「不要」設定這一項，留著註解就好。
#    未設定時 database.py 會用內建的 XAMPP 預設（root、無密碼、localhost），
#    那正是本機直接跑 uvicorn 時需要的值。
#
# ⚠️ 特別不要在這裡填 Docker 的位址（...@db:3306/...）。
#    容器那邊由 docker-compose.yml 以真正的環境變數注入，不需要本檔；
#    而填在這裡會讓本機直接跑的時候連到不存在的主機 db。
#    實際踩過：air_quality.py 會在 import 時呼叫 load_dotenv，
#    使得「誰先被 import」變成會影響資料庫連線的隱性相依。
#
# 只有在你的 MySQL root 有設密碼、或資料庫不在本機時才需要取消註解。
# DATABASE_URL=mysql+pymysql://root:你的密碼@localhost/pypoly_db
