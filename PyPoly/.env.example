# PyPoly 環境變數範本
# 使用方式：複製本檔為 .env（同目錄），填入實際值。.env 已被 .gitignore 忽略，不會進版控。

# JWT 簽章密鑰（請改成隨機長字串）
SECRET_KEY=change-me-to-a-random-secret

# Gmail SMTP（忘記密碼寄送 OTP 用）
# SENDER_PASSWORD 需使用 Google 帳戶的「應用程式密碼」，非登入密碼。
# ⚠️ 舊版曾把密碼寫死在 main.py，請至 Google 帳戶撤銷該密碼並重新產生後填入此處。
SENDER_EMAIL=your-service@gmail.com
SENDER_PASSWORD=your-google-app-password

# （選配）資料庫連線字串，未設定時使用 database.py 內建 XAMPP root 無密碼設定
# DATABASE_URL=mysql+pymysql://root:@localhost/pypoly_db
